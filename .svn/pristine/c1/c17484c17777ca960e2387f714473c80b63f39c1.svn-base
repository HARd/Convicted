using UnityEngine;
using System;
using System.Collections;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

public class FirstStartScreen : ScreenBase 
{
	[SerializeField]
	Text screenTextTop;

	[SerializeField]
	Text screenText;

	public void Start()
	{
		screenTextTop.text = Localization.Instance.GetLocale(779);
		screenText.text = Localization.Instance.GetLocale(780);
	}
}
